# Review Journal

This journal records the domain cases that matter before widening the public API.

The local checks classify each case as `ship`, `watch`, or `hold`. That gives the project a small review vocabulary that matches its graphics focus without claiming live deployment or external usage.

## Cases

- `baseline`: `geometry span`, score 197, lane `ship`
- `stress`: `atlas pressure`, score 167, lane `ship`
- `edge`: `shader drift`, score 163, lane `ship`
- `recovery`: `render budget`, score 255, lane `ship`
- `stale`: `geometry span`, score 191, lane `ship`

## Note

A future change should add new cases before it changes the scoring rule.
